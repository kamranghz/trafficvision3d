# TrafficVision3D runtime dependencies
# Install with: pip install -r requirements.txt

# Core CV / ML
ultralytics>=8.0.0
opencv-python>=4.8.0
numpy>=1.24.0
torch>=2.0.0
torchvision>=0.15.0

# Tracking and geometry
supervision>=0.15.0
lapx>=0.5.0
scipy>=1.10.0
transforms3d>=0.4.1

# Utilities
pyyaml>=6.0
dataclasses-json>=0.5.0
loguru>=0.7.0

# Optional inference backend
onnxruntime>=1.15.0

# Note:
# - Do not add asyncio/threading/queue here; they are Python standard library modules.
# - `pxr` (USD Python API) is typically provided by Omniverse/Isaac Sim environments.